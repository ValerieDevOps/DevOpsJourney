# 📘 Day 8 — Linux Networking & DNS Troubleshooting Lab

Script started on 2026-05-25 13:48:43+01:00 [TERM="xterm-256color" TTY="/dev/pts/3"]

================================================================================
1. IDENTIFY NETWORK INTERFACE
================================================================================

COMMAND:
ip a

OUTPUT SUMMARY:
1: lo: LOOPBACK interface
    inet 127.0.0.1/8
    inet6 ::1/128

2: eth0: ACTIVE NETWORK INTERFACE
    inet 172.24.107.115/20
    inet6 fe80::215:5dff:fe8d:7239/64
    MAC: 00:15:5d:8d:72:39

EXPLANATION:
- lo = internal system communication (localhost)
- eth0 = main network interface used for internet access
- inet = IPv4 address assigned to machine
- inet6 = IPv6 address
- MAC = physical/network hardware identifier


================================================================================
2. CHECK NETWORK ROUTE
================================================================================

COMMAND:
ip route

OUTPUT:
default via 172.24.96.1 dev eth0
172.24.96.0/20 dev eth0 proto kernel scope link src 172.24.107.115

EXPLANATION:
- default gateway = 172.24.96.1 (router)
- all external traffic goes through this gateway
- local subnet = 172.24.96.0/20


================================================================================
3. TEST GATEWAY CONNECTIVITY
================================================================================

COMMAND:
ping -c4 172.24.96.1

OUTPUT:
4 packets transmitted, 4 received, 0% packet loss

EXPLANATION:
- confirms local network is working
- gateway is reachable
- no packet loss = stable connection


================================================================================
4. CHECK DNS CONFIGURATION
================================================================================

COMMAND:
cat /etc/resolv.conf

OUTPUT:
nameserver 172.24.96.1

EXPLANATION:
- DNS server is the router (gateway)
- system uses it to resolve domain names (e.g. google.com)


================================================================================
5. TEST INTERNET DOMAIN RESOLUTION
================================================================================

COMMAND:
ping google.com

OUTPUT:
google.com resolved to IP (142.251.216.110)
some packet delay observed but successful replies received

EXPLANATION:
- DNS resolution works
- internet connection is active
- domain successfully mapped to IP address


================================================================================
6. CHECK LOCAL HOST FILE
================================================================================

COMMAND:
cat /etc/hosts

OUTPUT:
127.0.0.1 localhost
127.0.1.1 DESKTOP-MNEU0P5.localdomain

EXPLANATION:
- /etc/hosts = local DNS override file
- resolves names BEFORE external DNS is used
- used for localhost and manual mapping


================================================================================
7. INSTALL DNS TOOL (dig)
================================================================================

COMMAND:
sudo apt install bind9-dnsutils

ISSUE:
- package fetch failed (404 error)
- fixed using update

FIX COMMAND:
sudo apt update --fix-missing

EXPLANATION:
- updates package index
- fixes broken or missing repository links


================================================================================
8. CHECK LISTENING PORTS / DNS SERVICE
================================================================================

COMMAND:
ss -tuln

OUTPUT:
127.0.0.53:53  → systemd-resolved DNS service
127.0.0.54:53  → local DNS resolver

EXPLANATION:
- system runs internal DNS resolver (systemd-resolved)
- DNS queries handled locally before external lookup


================================================================================
9. TEST INVALID DOMAIN
================================================================================

COMMAND:
ping googllle.com

OUTPUT:
Temporary failure in name resolution

EXPLANATION:
- DNS cannot resolve incorrect domain name
- confirms DNS system is active and validating names


================================================================================
10. MODIFY HOSTS FILE (DNS OVERRIDE TEST)
================================================================================

COMMAND:
echo "1.2.3.4 google.com" | sudo tee -a /etc/hosts

EXPLANATION:
- manually maps google.com to fake IP 1.2.3.4
- overrides real DNS resolution


TEST:
ping google.com

RESULT:
google.com resolves to 1.2.3.4
100% packet loss

EXPLANATION:
- /etc/hosts overrides DNS completely
- system trusts local mapping first


================================================================================
11. TEST API CONNECTIVITY
================================================================================

COMMAND:
curl https://api.github.com/invalidendpoint

OUTPUT:
Could not resolve host

EXPLANATION:
- DNS resolution affected by hosts override or misconfiguration
- system cannot properly resolve domain


================================================================================
FINAL NETWORK FLOW UNDERSTANDING
================================================================================

APPLICATION (curl/ping)
        ↓
/etc/hosts (manual override)
        ↓
DNS resolver (/etc/resolv.conf)
        ↓
systemd-resolved service
        ↓
Gateway (172.24.96.1)
        ↓
Internet

================================================================================
KEY LEARNINGS

- ip a → shows network interfaces
- ip route → shows routing path
- ping → tests connectivity
- resolv.conf → DNS server config
- hosts → manual DNS override
- ss -tuln → shows active network services
- DNS failure = name resolution issue, not network failure
